# Tidy First

Kent Beck の "Tidy First"。変更を 2 種に分けて扱う: **構造変更**（振る舞いを変えない並べ替え＝リネーム / 抽出 / 移動）と **振る舞い変更**（機能追加・挙動変更）。

- 構造変更と振る舞い変更を同一コミットに混ぜない。両方必要なら構造変更を先に行う。
- 構造変更の前後でテストを走らせ、振る舞いが変わっていないことを確認する。
- どちらの変更かをコミットメッセージから判別できるようにする。

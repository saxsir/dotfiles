# 形名参同 (Verification by Declaration)

宣言（名）と実態（形）を一致させ、未確認の完了主張を防ぐ。

- **着手前**: 観測可能な完了条件を宣言する（通すべきテスト名・実行コマンド・確認する出力・閾値）。「動くようになる」等の曖昧な宣言は不可。宣言できないなら、できる粒度まで分解する。
- **完了主張前**: 宣言したコマンドを実行し、実出力を応答に含め、宣言と一致していることを示す。一致しなければ完了とせず再着手する。「動くはず」で締めない。
